@page
@using SkolefotograferneSemesterProjekt.Models
@model SkolefotograferneSemesterProjekt.Pages.Photographers.ViewPhotographerModel
@{
    ViewData["Title"] = "Fotograf info";
}

<h1>Viser @Model.Photographer.FirstName @Model.Photographer.Surname's information</h1>


<p>Email: @Model.Photographer.Email</p>
<p>Telefonnummer: @Model.Photographer.PhoneNumber</p>
<p>Hjemmeside: @Model.Photographer.Website</p>
<p>CVR: @Model.Photographer.CVR</p>
<p>Facebook: @Model.Photographer.Facebook</p>
<p>Instagram: @Model.Photographer.Instagram</p>

<form>
    <div>
        <a asp-page="ShowPhotographers" class="btn btn-primary">Tilbage til fotografer</a>
    </div>
</form>

@if (HttpContext.Session.GetInt32("Role") == 4)
{
    <form method="post">
        <td><button type="button" class="btn btn-danger" onclick="DisplayConfirmDeleteModal('@Model.Photographer.ID')">Slet</button></td>
    </form>
}
<form method="post" asp-page-handler="Delete" id="deleteForm">
    <input type="hidden" name="id" id="deleteId" />
</form>

<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">

            <div class="modal-header bg-warning">
                <h5 class="modal-title">Bekræft sletning</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body">
                <p>Er du sikker på, at du vil slette denne bruger?</p>
            </div>

            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    Annuller
                </button>

                <button type="button" class="btn btn-danger" id="confirmDeleteBtn">
                    Slet
                </button>
            </div>

        </div>
    </div>
</div>
@section Scripts {
    <script>
        function DisplayConfirmDeleteModal(id) {
            document.getElementById("deleteId").value = id;

            var modal = new bootstrap.Modal(document.getElementById('deleteModal'));
            modal.show();
        }

        document.getElementById("confirmDeleteBtn").addEventListener("click", function () {
            document.getElementById("deleteForm").submit();
        });
    </script>
}
